<% unless flash.empty? %>
  <script type="text/javascript">
    <% flash.each do |key, val| %>
      <% type = key.to_s.gsub('alert', 'error').gsub('notice', 'info') %>
     	toastr['<%= type %>']('<%= val %>');
    <% end %>
	</script>
<% end %>
